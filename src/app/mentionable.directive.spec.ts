import { MentionableDirective } from './mentionable.directive';

describe('MentionableDirective', () => {
  it('should create an instance', () => {
    const directive = new MentionableDirective();
    expect(directive).toBeTruthy();
  });
});
